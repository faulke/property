import * as actions from '../actions';

export const initialState = {
  property: null,
  isFetching: false
};

const addListing = (state = initialState, { type, payload }) => state;

export default addListing;
